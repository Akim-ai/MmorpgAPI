from django.apps import AppConfig


class HelpCodeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'

    name = 'HelpCode'
    label = 'HelpCode'

    verbose_name = 'Помогающий код'
